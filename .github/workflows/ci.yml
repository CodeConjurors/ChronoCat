name: CI

on:
  push:
    branches: [citest]
    paths-ignore: ['frontend/**']

jobs:
  deployment_pipeline:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout the repository
        uses: actions/checkout@v4
